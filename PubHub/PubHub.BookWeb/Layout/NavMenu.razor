<nav class="navbar navbar-expand-sm navbar-toggleable-sm navbar-light header-template border-bottom box-shadow mb-3">
    <div class="container">
        <a class="navbar-brand text-white" href=""><b>Pub</b><span style="color: #7E7ECB;"><b>Hub</b></span></a>
        <button class="navbar-toggler" type="button" @onclick="ToggleNavMenu">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="@NavMenuCssClass " @onclick="ToggleNavMenu">
            <ul class="navbar-nav flex-grow-1">
                <li class="nav-item p-2">
                    <NavLink class="nav-link text-dark" href="" Match="NavLinkMatch.All">
                        <span class="oi oi-home" aria-hidden="true"></span> Front page
                    </NavLink>
                </li>
                <li class="nav-item p-2">
                    <NavLink class="nav-link text-dark" href="counter">
                        <span class="oi oi-plus" aria-hidden="true"></span> Book search
                    </NavLink>
                </li>
                @* TODO: Only add if a user is logged in (AuthorizeView -> Microsoft.AspNetCore.Components.Authorization) *@
                @if (true)
                {
                    <li class="nav-item p-2">
                        <NavLink class="nav-link text-dark" href="personalLib">
                            <span class="oi oi-plus" aria-hidden="true"></span> Personal library
                        </NavLink>
                    </li>
                }
            </ul>
        </div>
        <div class="navbar-collapse justify-content-end">
            <ul class="navbar-nav">
                <li class="nav-item p-2">
                    @* TODO: Add check if user is logged in (AuthorizeView -> Microsoft.AspNetCore.Components.Authorization) *@
                    @if (true)
                    {
                        <NavLink class="nav-link text-dark" href="signin" Match="NavLinkMatch.All">
                            <span class="oi oi-home" aria-hidden="true"></span> Sign in
                        </NavLink>
                    }
                    else
                    {
                        <NavLink class="nav-link text-dark" href="signout" Match="NavLinkMatch.All">
                            <span class="oi oi-home" aria-hidden="true"></span> Sign in
                        </NavLink>
                    }
                </li>
            </ul>
        </div>
    </div>
</nav>

@code {
    bool collapseNavMenu = true;

    string baseMenuClass = "navbar-collapse d-sm-inline-flex flex-sm-row-reverse justify-content-start";

    private bool expandOptionNav;

    string NavMenuCssClass => baseMenuClass + (collapseNavMenu ? " collapse" : "");

    void ToggleNavMenu()
    {
        collapseNavMenu = !collapseNavMenu;
    }
}
